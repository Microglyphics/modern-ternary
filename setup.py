# TernaryChartProject/setup.py
from setuptools import setup, find_packages

setup(
    name="ternarychart",
     version="1.2.0",  # Update this to the new version
    packages=find_packages(),
)